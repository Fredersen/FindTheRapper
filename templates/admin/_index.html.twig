<div class="sidebar d-flex flex-column vh-100 flex-shrink-0 p-3 text-white bg-dark"> <a href="{{ app.user.game ? path('game', {'id': app.user.game.id}) : path('app_logout') }}" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-white text-decoration-none"> <svg class="bi me-2" width="40" height="32"> </svg> <span class="fs-4">Find The Rapper</span> </a>
    <hr>
    <ul class="nav nav-pills flex-column mb-auto">
        <li> <a href="{{ path('admin_game_index') }}" class="nav-link {{ app.request.pathInfo == path('admin_game_index') ? 'active' : 'text-white' }}"> <i class="fa fa-first-order"></i><span class="ms-2">Liste des niveaux</span> </a> </li>
        <li> <a href="{{ path('admin_game_new') }}" class="nav-link {{ app.request.pathInfo == path('admin_game_new') ? 'active' : 'text-white' }}"> <i class="fa fa-dashboard"></i><span class="ms-2">Creer un niveau</span> </a> </li>
        <li> <a href="#" class="nav-link text-white"> <i class="fa fa-cog"></i><span class="ms-2">Statistiques</span> </a> </li>
        <li> <a href="{{ path('app_logout') }}" class="nav-link text-white"> <i class="fa fa-cog"></i><span class="ms-2">Se déconnecter</span> </a> </li>
    </ul>
    <hr>
</div>
